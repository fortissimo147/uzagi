# Typhoon Escape — Globe Edition 設計書

対象: `lovewcycle.com/games/others/typhoon-escape.html` の再現版。
舞台を日本から**台湾**へ、平面メルカトルから**3Dグローブ（地球儀）**へ置き換え、
ゲーム内の文言はすべて英語にする。

ラベル: **[確認済]** ツール（元ソース読解・Node実測）で検証済 /
**[推定]** 根拠はあるが未検証 / **[不明]** 要確認。

---

## 0. 前提

### 0.1 元ゲームの仕様は入手済み **[確認済]**

`https://lovewcycle.com/games/others/typhoon-escape.html` への直接取得は本セッションの
egressポリシーでブロックされた（`EGRESS_BLOCKED`）ため、**ユーザーから元HTMLの全文提供を受けた**。
以降の §1 は、その 774 行の `<script>` を読み取った**実仕様**であり、推測ではない。

元HTMLから機械的に確認した事実:

| 項目 | 値 | 確認方法 |
| --- | --- | --- |
| 埋め込み地図データ | TopoJSON、`objects = {countries, land}`、arcs 595本 | JSONパース |
| `countries` の中身 | **地物1件のみ・`id="392"`（日本）** | JSONパース |
| 元データの出所 | **`world-atlas` の `countries-110m` / `land-110m`** | 日本の頂点数65・リング3・bbox 129.409–145.544 / 31.030–45.551 が `countries-110m.json` と完全一致 |
| クレジット | Natural Earth（パブリックドメイン）+ world-atlas ISC + 気象庁アジア名リスト | メニューDOM |

つまり **§3 で選んだデータ源は元ゲームと同一**である。差分は解像度の選択だけ。

### 0.2 確定済みの決定（ユーザー回答）

| 項目 | 決定 |
| --- | --- |
| 仕様の確定方法 | 元HTMLソースの提供（最も忠実） → **完了** |
| コードの置き場所 | `games/typhoon-escape/` に独立プロジェクト（既存ゲームと完全分離） |
| 世界地図の描画 | Natural Earth のベクタ国境をビルド時に焼き込む（画像ファイル不使用） |

### 0.3 設計の中心方針 — ルール層と世界層の分離

ルールは確定したが、**平面 → 球面の移植で最も壊れやすいのは「ルールが座標系を直接触る」構造**である。
元ゲームは `jp.x += vx * sp * dt` のように px 座標を直接いじっており、これをそのまま持ち込むと
球面化で全滅する。依存を一方向に固定し、ルールは「面(Surface)」の抽象だけを触る。

```
rules/   ← 勝敗・スコア・スポーン・難度曲線（元ゲームの定数をそのまま持つ層）
   ↓ 依存
world/   ← 球面座標・大圏移動・台風の運動・地図データ（座標系の詳細を隠す層）
   ↓ 依存
render/  ← three.js の描画とカメラ（ゲーム内容を一切知らない）
```

`rules/` は `world/` の API（`Surface`, `Storm`, `Body`）だけを触る。
`world/` は `rules/` を import しない。この向きを `npm test` の静的チェックで守る。

---

## 1. 元ゲームの実仕様 **[確認済: 元ソース読解]**

以下はすべて元 `<script>` からの直接引用・実測値。**推測は含まない。**

### 1.1 基本構造

- 論理ビューポート `W=400, H=700`、基準寸法 `M=700`。実画面へは `SC = min(VW/W, VH/H)` でレターボックス。
- 投影は**メルカトル**。`CEN=170, LONW=110, LATN=75, LATS=-60`。
  `px(lon) = W/2 + (wrap(lon)-CEN)·W/LONW` / `py(lat) = (MY-merc(lat))/(MY-MN)·H`。
  → 画面 x∈[0,400] は **経度 115°E–225°E**（＝135°W）に対応。1° 経度 = 3.6364 px **[確認済: 計算]**
- プレイヤーは**日本列島そのもの**。`jp = {x, y}` を動かす。列島のリングは `jp` からの相対座標で
  `K=1.6` 倍に拡大して保持（`buildMap()` で `px(138), py(37)` を原点に正規化）。
- **開始位置は `px(144), py(37)`** — つまり形状の基準経度(138)より東に 6° ずれた位置から始まる。
- `jp.x` は `[30, 370]`、`jp.y` は `[30, 670]` にクランプ。**プレイヤーはビューポートの外へ出られない。**

### 1.2 ゲームループとスコア

| 項目 | 実仕様 |
| --- | --- |
| dt | `min(0.05, (now-last)/1000)` — 1フレーム最大 50 ms |
| 時間 | `elapsed += dt` |
| 暦 | `START = new Date(2026,7,1)` = **2026-08-01**、`gameDate()` は `START + floor(elapsed)` **日** |
| **時間スケール** | **実時間 1 秒 = ゲーム内 1 日** |
| スコア | **生存日数** `Math.floor(elapsed)` |
| 敗北 | **接触即死**（1回でも当たれば終了） |
| クリア条件 | **なし。エンドレス。** |
| 敗北演出 | `over` 後 `dt *= 0.3` のスローモーションを 1500 ms、その後ポップアップ |

### 1.3 当たり判定 **[設計上もっとも重要]**

```js
for (i of ty) for (j of jpPts)
  if (Math.hypot(jp.x + jpPts[j][0] - ty[i].x, jp.y + jpPts[j][1] - ty[i].y) < ty[i].r * 0.55)
```

- 判定対象は**海岸線の全頂点**（日本110mで 65 点）。塗りの内側は判定しない。
- 致死半径は**外側の黄色い円 `r` ではなく、内側の赤い円 `r × 0.55`**。
- 上陸地域は、**当たった頂点の元の経緯度** `jpPts[j][2], jpPts[j][3]` を `region()` に渡して決める。

### 1.4 台風の運動 — 5つの進路パターン

`spawn()` が毎回このうち1つを等確率で選ぶ。

| name | segMin | segMax | jitter | pull | spd | stall | bias |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 直進 | 4 | 7 | 0.3 | 0.2 | 1.2 | 0 | — |
| 迷走 | 0.7 | 1.6 | 2.4 | 0.2 | 0.9 | 0 | — |
| 転向 | 1.5 | 3 | 0.3 | 0.2 | 1.0 | 0 | `±(0.5–1.0)` 発生時に固定 |
| 追尾 | 1.5 | 3 | 0.6 | 0.9 | 0.8 | 0 | — |
| 停滞 | 1.5 | 3 | 1.0 | 0.4 | 1.3 | 0.5 | — |

`timer` が尽きるたびに1回だけ進路を更新する（毎フレームではない）:

```js
d = wrapAngle(toward(player) - t.ang);
t.ang += d * pat.pull + (Math.random() - 0.5) * pat.jitter + (pat.bias || 0);
t.timer = segMin + rand*(segMax - segMin);
t.trail.push([t.x, t.y]);
```

- `pull` が**プレイヤー追尾の強さ**、`jitter` が**ランダム偏向**、`bias` が**一方向への転向癖**。
- `stall > 0` のパターンは、進路更新時に確率 `stall` で「停滞」へ入り（`timer = 1–2.5 s`）、
  速度が `tsp * 0.15` に落ちる。次の更新で必ず解除。
- 個体差 `t.spd = pat.spd * (0.8 + rand*0.4)`。
- 発生直後の向きは `toward(player) + (rand-0.5)*0.8` — **必ずプレイヤー方向を向いて生まれる。**

### 1.5 台風の強度と寿命

| 変数 | 実仕様 |
| --- | --- |
| 初期半径 | `r = M*0.015 = 10.5 px` |
| ピーク半径 | `rPeak = M*0.05 + rand*M*0.035` = **35–59.5 px** |
| 発達時間 | `growT = 3 + rand*3` 秒（＝ゲーム内 3–6 日） |
| 発達 | `r += rPeak/growT * dt`（`rPeak` で頭打ち） |
| 衰弱開始 | ピーク到達後、毎フレーム確率 `dt * 0.06`（≒ 期待値 16.7 秒） |
| 衰弱 | `r -= rPeak*0.12 * dt` |
| 寿命 | `life = 12 + rand*10` 秒。**残り 5 秒を切ると強制衰弱** |
| 消滅 | `r <= M*0.012 (=8.4 px)` または `life <= 0` → 「温帯低気圧に変わりました」 |
| 画面外消滅 | 一度画面内に入った後に `r` 分はみ出す、または常に ±150 px 超 → 「遠ざかり、消滅しました」 |

### 1.6 速度と難度曲線 **[確認済: 計算]**

| 項目 | 式 | 値 |
| --- | --- | --- |
| プレイヤー速度 | `M*0.125` | 87.5 px/s（一定） |
| 台風基準速度 | `tsp = M*0.09 + elapsed*M*0.0035` | t=0 で 63 px/s、**毎ゲーム内日 +2.45 px/s** |
| 実速度 | `tsp * t.spd`（停滞中は `tsp * 0.15`） | |
| **速度逆転** | `0.125 = 0.09 + 0.0035·t` | **t = 10.0 ゲーム内日で台風がプレイヤーより速くなる** |
| 発生間隔 | `max(1.5, 4 - elapsed*0.08)` | 4 s → **t = 31.25 日で下限 1.5 s** |
| 同時数上限 | 7 | |
| 発生位置 | 4辺のいずれか、画面外 60 px | |

**この「10日で追い越される」設計が本作の核**である。逃げ切りは不可能で、
プレイヤーにできるのは「かわす」ことだけ。だから生存日数がスコアになる。

### 1.7 予報円（進路予想）の描画

`setForecast()` が `timer` 更新時に再計算する。

```
v  = 停滞中 ? tsp*0.15 : tsp*t.spd
T  = t.timer                        // 次の進路更新までの残り時間
r  = 衰弱中/残5秒以内 ? r - rPeak*0.12*T : min(rPeak, r + rPeak/growT*T)
(fx, fy) = 現在位置 + 現在の向き × v × T
fr = max(M*0.012, r) * 0.55         // 予報円の半径＝将来の致死半径
```

描画は 4 要素: 中心への破線、`fr*0.5` の破線円、`fr` の赤い実線円、
`len > fr - r*0.55` のとき現在の致死円と予報円の**共通外接線 2 本**。
過去進路は白の実線ポリライン＋各節点に半径 2.5 の点。

**つまり予報円は「次の進路更新までは確実にこう動く」という正直な情報**であり、
プレイヤーはこれを読んで回避する。ここがゲーム性の中心なので、球面化でも厳密に維持する。

### 1.8 大陸を押しのける機構

```js
pushLand(dt):  各 body について、jp が AABB±120px 内なら
               日本の全頂点で outer リングに対する ray-casting 判定、
               内側にある頂点の相対座標を合計 (sx,sy) し、
               B.v = normalize(sx,sy) * M*0.125
slideLand(dt): B.o += B.v*dt;  B.v *= Math.pow(0.12, dt);  |B.v|<0.5 で停止
```

- 押す向きは**日本の中心から見た貫入点の方向**。＝ 大陸は日本から押しのけられる。
- `land-110m` の各ポリゴンが body。**南極大陸だけ除外**（`maxLat < -55`）。
- 減衰係数 `0.12^dt` はフレームレート非依存。押した後も慣性で滑る。

### 1.9 UI・演出

| 要素 | 実仕様 |
| --- | --- |
| HUD | 上部に日付のみ（`2026年8月1日`） |
| ニュースティッカー | 画面下に流れる行。発生＝赤、消滅＝黄。`animationDuration = dist/300` s |
| ティッカーと操作 | 行が増えると CSS 変数 `--rows` が増え、仮想スティックが `16 + rows*36 px` 持ち上がる |
| 操作 | ティッカー/ポップアップ/メニュー以外の**任意の場所**を押すとそこに仮想スティック（半径44）が生まれる |
| キーボード | WASD / 矢印。8方向の単位ベクトルではなく `vx,vy ∈ {-1,0,1}` の生値（斜めは √2 倍速い） |
| 台風名 | 気象庁アジア名 140 個。開始時に `nameIdx = rand*140` でランダム開始、以後は順送り |
| 台風番号 | `tyNo` はゲーム開始からの通し番号（1号, 2号, …） |
| シェア | X(Twitter) intent。日付・台風名・上陸地域・生存日数を含む |
| 開始 | ボタンで `requestFullscreen()` + `screen.orientation.lock('portrait')` |
| 色 | 海 `#1B2F5E` / 枠外 `#0E1A36` / 自国 `#8FBF7F`+白線 / 他大陸 `#4E6656`+`#7C9A86` / 外円 `#FFE000` / 内円 `#E61E2D` |

### 1.10 上陸地域の判定（元）

`region(lon, lat)` は 9 地域を**経緯度のしきい値のカスケード**で判定する。
行政界ではなく、ゲーム用の粗い幾何近似。

```
lat>=41.3 → 北海道 / lon>=139.3 && lat>=37 → 東北 / lon>=138.4 && lat>=34.8 → 関東
lon 132.4–134.8 && lat 32.7–34.45 → 四国 / lon>=136.5 → 中部 / lon>=134.2 → 近畿
lon>=130.8 && lat>=34 → 中国 / lat>=30 → 九州 / else → 沖縄・南西諸島
```

---

## 1b. 日本 → 台湾・平面 → 球面の変換 **[確認済: Nodeで実測・検算]**

### 1b.1 何を保存し、何を変えるか

元ゲームの手触りを決めているのは**絶対値ではなく比**である。
以下の 3 つの無次元比を**厳密に保存**し、それ以外（画面px・投影）を捨てる。

| 保存する比 | 元の値 |
| --- | --- |
| 致死半径(ピーク最大) / 自国の外接半径 | 0.472768 |
| プレイヤー速度 / 自国の外接半径 [/s] | 1.264085 |
| プレイ窓の幅 / 自国の外接半径 | 5.778673 |

### 1b.2 換算定数の導出

実測値 **[確認済]**:

| 量 | 値 |
| --- | --- |
| 日本の真の外接角半径（110m, 球面重心基準） | 9.5020° = 1056.5 km |
| 台湾の真の外接角半径（110m） | 1.9207° = 213.6 km |
| 日本の最大大圏スパン | 17.618° = 1959.1 km |
| 台湾の最大大圏スパン | 3.396° = 377.6 km |
| **サイズ比 日本/台湾** | 外接半径 4.947 倍 / スパン 5.188 倍（5%以内で一致） |
| 元マップの弧長スケール @lat37 | `(400/110)/cos37° = 4.5532 px/度` |
| 元ゲームでの日本の描画外接半径 | `9.5020 × 4.5532 × 1.6 = 69.22 px` |

台湾も**元と同じ `K=1.6` 倍**で描く（＝周囲の大陸に対して 1.6 倍の誇張。元ゲームの視覚文法をそのまま踏襲）。
→ 描画外接角半径 `1.9207 × 1.6 = 3.0732°` = 341.7 km。

したがって換算定数は

```
A = 3.0732° / 69.22 px = 0.044397 度(弧長) / 元px
```

### 1b.3 換算表（この表が実装の `rules/config.js` になる）

**この表は `tools/calibrate.mjs` が world-atlas の実データから生成する** **[確認済: 実行済]**。
手で書いた数値ではない。`npm i` 後に `node tools/calibrate.mjs` で再現・検証できる。

1 実秒 = ゲーム内 1 日 なので、`/s` は `/日` と読み替えられる。

| 項目 | 元 [px] | 球面 [°] | [km] | ゲーム内 |
| --- | ---: | ---: | ---: | --- |
| 台風 初期半径（外） | 10.5 | 0.4662 | 51.8 | |
| 台風 ピーク半径（外）最小 | 35 | 1.5539 | 172.8 | |
| 台風 ピーク半径（外）最大 | 59.5 | 2.6416 | 293.7 | |
| **致死半径 ピーク最小** | 19.25 | 0.8546 | **95.0** | |
| **致死半径 ピーク最大** | 32.725 | 1.4529 | **161.6** | |
| 消滅しきい値（外） | 8.4 | 0.3729 | 41.5 | |
| **プレイヤー速度** | 87.5 /s | 3.8848 /s | 432.0 /日 | **18.0 km/h** |
| 台風速度 t=0 | 63 /s | 2.7970 /s | 311.0 /日 | **13.0 km/h** |
| 台風加速 | 2.45 /s² | 0.1088 /s² | 12.1 /日² | |
| 台風速度 t=30日 | 136.5 /s | 6.0602 /s | 673.9 /日 | **28.1 km/h** |
| 停滞時速度 t=0 | 9.45 /s | 0.4196 /s | 46.7 /日 | 1.9 km/h |
| 押しのけ速度 | 87.5 /s | 3.8848 /s | 432.0 /日 | |
| 押しのけ判定余白 | 120 | 5.3277 | 592.4 | |
| 滑走停止しきい値 | 0.5 /s | 0.0222 /s | 2.5 /日 | |
| プレイ窓 幅 | 400 | 17.7589 | 1974.7 | |
| プレイ窓 高さ | 700 | 31.0781 | 3455.7 | |
| 発生マージン | 60 | 2.6638 | 296.2 | |
| 強制消滅マージン | 150 | 6.6596 | 740.5 | |

**副産物として物理的に妥当な値になる** **[推定: 実統計との突き合わせは未実施]**:
致死半径（＝暴風域相当）95–162 km、外側半径（＝強風域相当）173–294 km、
移動速度 13 km/h（発生直後）→ 28 km/h（30日後）。
実在の台風統計に寄せたわけではなく、**元ゲームの比を保存したら偶然この範囲に落ちた**。
実統計を名乗らせるなら中央気象署(CWA)／気象庁の公開資料を引いて明記すること。

半球横断は `180/3.8848 = 46.3` ゲーム内日、赤道一周は 92.7 日 **[確認済: 計算]**。
生存日数スコアと噛み合うスケール。

### 1b.4 球面化で置き換えが必要な 5 箇所

| 元の機構 | 平面前提 | 球面での置き換え |
| --- | --- | --- |
| `jp.x/y` のクランプ | ビューポートが世界の全て | **クランプを撤廃。地球全体がプレイ領域**（＝「世界地図完成版」の実体） |
| 4辺からのスポーン | 画面に辺がある | **カメラ可視円の外周リング上に一様分布**。角距離 = 可視角半径 + 2.664° |
| 画面外での消滅 | 矩形との比較 | **プレイヤーからの角距離**で判定（可視角半径 + マージン） |
| `pushLand` の ray-casting | 平面多角形 | 大陸重心を中心とする**心射方位図法へ局所投影**して同じ ray-casting。相互作用は 5.3° 以内の局所現象なので歪みは無視できる |
| `B.ox/oy` の平行移動 | 2Dベクトル | **回転クォータニオン**。押す向き＝大圏方向、角速度ベクトルを `0.12^dt` で減衰 |

`toward()`（プレイヤー方位）と `t.ang` は、球面では
**台風位置における接平面上の方位角**として定義し直す（`§2.3` の `(p, t)` 対）。
`wrapAngle` は `atan2(sin d, cos d)` のまま使える。

### 1b.5 上陸地域（台湾版） **[推定: 幾何近似であり行政界ではない]**

元と同じ「経緯度しきい値のカスケード」方式。`countries-10m` の台湾 558 頂点で分布を検証済 **[確認済]**。

```
lon < 119.0                     → the Kinmen Islands
lon < 120.0                     → the Penghu Islands
lon >= 121.3 && lat < 23.0      → Eastern Taiwan      // 蘭嶼・緑島
lat >= 24.5                     → Northern Taiwan
lat >= 24.3 && lon >= 121.4     → Northern Taiwan     // 宜蘭
lon >= 120.9 && lat >= 22.6     → Eastern Taiwan      // 花蓮・台東
lat >= 23.5                     → Central Taiwan
else                            → Southern Taiwan
```

頂点分布 **[確認済]**: Southern 161 (28.9%) / Northern 147 (26.3%) / Eastern 95 (17.0%) /
Penghu 77 (13.8%) / Central 48 (8.6%) / Kinmen 30 (5.4%)。**全地域が到達可能。**

主要都市の座標を通した結果 **[確認済: 分類の実行]**: 台北・基隆・宜蘭・新竹→Northern、台中→Central、
花蓮・台東・蘭嶼・緑島→Eastern、台南・高雄・恆春・嘉義→Southern、澎湖→Penghu、金門→Kinmen。
（都市座標そのものは記憶に依るため **[推定]**。頂点分布のほうは実データなので **[確認済]**。）

このカスケードと分布検査は `tools/calibrate.mjs` に実装済で、実行して再現できる。

注意: これは**ゲーム用の幾何近似**であり、中華民國の統計地區標準分類そのものではない。
公式区分に合わせるなら出典を引いて再定義する（§11-2）。

なお `countries-10m` の台湾ジオメトリは緯度上限が **25.287°** であり、
それより北にある馬祖列島は**データに含まれない** **[確認済: 実測]**。したがって地域名にも登場しない。

---

## 2. 座標系と球面数学（**確定** / 検算済）

### 2.1 単位と変換 **[確認済: Nodeで往復検算]**

すべての位置は `{lat, lon}`（度）で持ち、描画直前に単位ベクトルへ落とす。

```
lat = φ, lon = λ （ラジアン）
x =  cos φ · sin λ
y =  sin φ
z =  cos φ · cos λ        // 経度0が +Z を向く。北極が +Y。
```

逆変換 `lat = asin(y)`, `lon = atan2(x, z)`。
台北 (25.0330, 121.5654) の往復で誤差 0（倍精度で 25.033000 / 121.565400 に復帰）。

### 2.2 距離

球面上の2点間は `d = R · acos(clamp(a·b, -1, 1))`、`R = 6371.0088 km`（IUGG 平均半径）。
検算値 **[確認済]**:

| 区間 | 本設計の計算値 |
| --- | --- |
| 台北 → 高雄 | 296.8 km |
| 台北 → マニラ | 1161.7 km |
| 台北 → グアム | 2751.5 km |

**注意**: 距離の計算そのものは検算済だが、**都市の座標は記憶に依るもの** **[推定]**。
実装時に地名を出すなら座標の出典を明記する。ゲーム内では地名を使わないので影響はない。

### 2.3 移動 — 大圏上の厳密ステップ（**設計の要**）

「緯度経度に直接足す」方式は高緯度で速度が狂い、極で破綻する。採らない。
位置 `p`（単位ベクトル）と進行方向 `t`（`p` に直交する単位接ベクトル）を組で持ち、
角度 `θ = v·dt / R` だけ回す:

```
p' = p·cos θ + t·sin θ
t' = -p·sin θ + t·cos θ
```

この形は正規直交性を保つので、緯度によらず速度が一定になり、極も普通に通過できる。
浮動小数の蓄積誤差だけ毎フレーム再正規化する（`t -= p(p·t)`, then normalize）。

旋回は `t` を `p` 軸まわりに回すだけ:  `t' = rotateAround(t, p, ω·dt)`。

台風の進路も同じ機構で表現する（進路の曲がり＝`ω` の時間変化）。

---

## 3. 地図データ（**確定** / 実データで検証済）

### 3.1 出典とライセンス **[確認済]**

- npm `world-atlas@2.0.2` — ライセンス **ISC**（Copyright 2013-2019 Michael Bostock）。
  中身は **Natural Earth v4.1.0** ベクタデータの TopoJSON 再配布。
  Natural Earth 本体はパブリックドメイン。**元ゲームが使っているのと同一のパッケージ** **[確認済: §0.1]**
- npm `topojson-client@3.1.0` — ライセンス **ISC**。デコードにビルド時のみ使用。

いずれも devDependency に置き、**実行時には持ち込まない**（焼き込んだ後は不要）。
`LICENSE` と出典表記を `games/typhoon-escape/CREDITS.md` と、
ゲーム内メニュー（元と同じ ☰ ボタン）に英語で載せる。

### 3.2 データ実測値 **[確認済]**

| ファイル | 地物 | リング数 | 頂点数 |
| --- | --- | --- | --- |
| `land-110m.json` | 1 | 126 | 5,123 |
| `land-50m.json` | 1 | 1,421 | 60,629 |
| `countries-110m.json` | 177 | — | — |
| `countries-50m.json` | 241 | 1,629 | 99,539 |

台湾（`id=158`）の解像度別 **[確認済]**:

| 解像度 | リング | 頂点 | 経度 | 緯度 | 備考 |
| --- | ---: | ---: | --- | --- | --- |
| 110m | 1 | **9** | 120.106–121.953 | 21.970–25.296 | 本島のみ。多角形が粗すぎて使えない |
| 50m | 2 | 68 | 118.288–121.928 | 21.925–25.277 | 金門・澎湖を含む。元の日本(65点)と同程度 |
| **10m** | 8 | **558** | 118.281–122.007 | 21.905–25.287 | 本島404 + 澎湖 + 金門 + 蘭嶼 + 緑島。**採用** |

**採用理由**: 元ゲームは自国の海岸線頂点を**そのまま当たり判定に使う**（§1.3）。
50m の台湾は 68 点＝頂点間隔 約20 km で、発生直後の致死半径 28.5 km（§1b.3）と同オーダーになり、
台風が頂点の隙間をすり抜けうる。10m なら本島 404 点で隙間が生じない。
コストは 558点 × 7個 = 3,906 回/フレームの距離計算で、無視できる。
（10m 台湾に**馬祖は含まれない** **[確認済]**。）

### 3.3 焼き込み方式

元ゲームと違い、プレイヤーはビューポートに閉じ込められず**地球全体を移動する**（§1b.4）。
したがって「台湾周辺だけ精細」という二層構造は成立しない。全球を一様に持つ。

| 層 | 元データ | 用途 | 量子化 | 実サイズ |
| --- | --- | --- | --- | --- |
| `player` | `countries-10m` の `id=158` | 自国の描画＋**当たり判定頂点** | Int16 / 台湾bbox ローカル | 558 pt × 4 B = **2.2 KiB** |
| `land` | `land-50m` | 他大陸の描画＋**押しのけ判定** | Int16 / 全球 | 60,629 pt × 4 B = **237 KiB** |

量子化ステップ **[確認済: 計算]**:
- 全球 Int16: 経度 360/65535 = 0.005493° ≈ **612 m** / 緯度 **306 m**。
  他大陸は最寄でも 592 km 先（押しのけ判定余白）でしか触らないので十分。
- 台湾 bbox ローカル Int16: 経度 **6.3 m** / 緯度 **5.7 m**。カメラを寄せても破綻しない。

合計 239 KiB（Base64 で約 319 KiB）。
参考: **元ゲームの HTML は TopoJSON を生テキストで埋め込んでおり 3.36 MB** **[確認済: 実測]**。
10倍以上小さくなる。

`land-110m`（20 KiB）は使わない。50m を採るのは、球面へ寄ったときに 110m の粗さが
そのまま輪郭のカクつきとして出るため。押しのけ判定のリング数は 126 → 1,421 に増えるが、
元と同じ AABB による早期棄却（`§1.8`）でほぼ全てが弾かれる。

**南極大陸の除外**は元と同じ規則をそのまま使う（各ポリゴンの外リングで `maxLat < -55`）。

### 3.4 パイプライン

```
npm run bake:geo
  → tools/bake-geo.mjs
      node_modules/world-atlas/{land-50m,countries-10m}.json を topojson-client でデコード
      → 台湾抽出 / 量子化 / Int16Array 化
      → src/data/geo.js を生成（Base64 文字列 + リングオフセット表）
```

生成物 `src/data/geo.js` は **リポジトリにコミットする**。
理由: 実行時・ビルド時ともにネットワーク不要にし、1ファイル完結版を成立させるため。
（既存リポジトリの `voicebank.js` と同じ「生成物をコミットし、手で編集しない」方式に合わせる。）

---

## 4. 描画（**確定**）

three.js。画像ファイルは一切使わず、すべてコード生成する（既存リポジトリの方針を踏襲）。
色は元ゲームの値をそのまま使う（§1.9）。

### 4.1 グローブの構成

1. **海** — `IcosahedronGeometry(R, 5)` に `#1B2F5E`。緯度方向に淡いグラデーション。
   背景（球の外）は `#0E1A36`。
2. **他大陸の塗り** — `land` データを球面三角形分割して `Mesh` にする。
   `#4E6656` 塗り、`#7C9A86` の輪郭線。**body ごとに別 Mesh** にし、
   押しのけの回転クォータニオンを `Object3D.quaternion` に直接入れる（§1b.4）。
3. **自国（台湾）の塗り** — `player` データを同様に。`#8FBF7F` 塗り、白 `#FFFFFF` の輪郭。
   `K=1.6` 倍に膨らませた形状（球面重心からの角距離を 1.6 倍する）で焼く。
   こちらも独立した `Object3D` で、位置クォータニオンだけを毎フレーム更新する。
4. **海岸線（線）** — 塗りと同じデータを `THREE.LineSegments` で半径 `R×1.0005` に重ねる。
   ベクタなので**ズームに対して常に鮮鋭**。塗りのテッセレーション誤差を線が隠す。

### 4.2 大気

`BackSide` の球を一回り大きく重ね、視線と法線の内積によるフレネル項で加算合成。
シェーダ数行で済み、画像もポストプロセスも不要。

### 4.3 台風の見た目 — **見た目と判定を一致させる**

元ゲームの描画要素を球面へ 1:1 で移す。ここは省略しない。ゲーム性の中心だから（§1.7）。

| 要素 | 元 | 球面での実装 |
| --- | --- | --- |
| 外円（黄 `#FFE000`, 塗り50%） | `arc(x,y,r)` | 地表に張り付く角半径 `r` の小円リング |
| **内円（赤 `#E61E2D`, 塗り75%）＝当たり判定** | `arc(x,y,r*0.55)` | 同、角半径 `0.55r`。**判定と完全に同一の値を描く** |
| 中心の白い × | 2本の線 | 中心にビルボードの × |
| 過去進路 | 白実線 + 節点の点 | 節点を大圏で結んだ `Line` + 各節点に小円 |
| 予報の破線 | `setLineDash([5,4])` | 大圏に沿った破線ジオメトリ（線分を等間隔に間引く） |
| 予報円 `fr` | 赤い実線円 | 角半径 `fr` の小円 |
| 予報円 `fr*0.5` | 白い破線円 | 同 |
| 共通外接線 2 本 | `acos((rs-rw)/len)` | **接平面で同じ式を解いてから球面へ写す**。局所現象なので歪みは無視できる |

渦そのものは手続き生成: 対数螺旋 `r(θ) = a·e^(bθ)` を球面へ写したリボンを数本、
中心軸まわりに反時計回り（北半球の実回転向き）で回す。中心に眼の穴。
**ただし渦は装飾**であり、判定は上表の内円だけが持つ。

台風が画面外にいる間は、画面端に方位インジケータを出す（元にはない。球面では
「画面外」が常態になりうるので追加が必要）。

---

## 5. カメラ（**確定** — 「動く時に適宜画面を動かす」要件の実装）

### 5.1 追従フレーム — **北を上に固定する**

元ゲームは常に北が上のメルカトルであり、プレイヤーの向きという概念がない
（`vx,vy` は速度そのもの。§1.9）。この性質は**そのまま維持する**。

したがってカメラは「プレイヤーの進行方向」を追わない。追うのは**位置だけ**:

```
up      = 台湾の中心 p の地表法線（＝ p 自身）
north   = 正規化( ŷ - p(p·ŷ) )      // 北極方向を接平面へ射影
right   = north × p
```

この (right, north, p) から目標クォータニオンを作り、
`slerp(current, target, 1 - exp(-k·dt))` で追従する。
`exp` 形にするのはフレームレート非依存にするため（`lerp(a,b,k·dt)` は可変fpsで挙動が変わる）。

**極付近**では `ŷ - p(p·ŷ)` が縮退するので、`|p·ŷ| > 0.999` のときは
直前フレームの `north` を保持してロールを凍結する（極を通過しても絵が回らない）。

### 5.2 ズームと自動フレーミング

基準画角は §1b.3 の「プレイ窓 17.76° × 31.08°」。縦長の元ゲームと同じアスペクトを保つ。

| 段 | 可視角半径 | 使う場面 |
| --- | --- | --- |
| WORLD | 90°（地球全体） | タイトル、ゲームオーバー後の引き |
| **PLAY** | **15.5°**（＝プレイ窓の縦半分） | **通常プレイ。元ゲームと同一の画角** |
| CLOSE | 8° | 台風が至近で、細部を見せたい時 |

**自動フレーミング**: プレイヤーと「最も危険な台風」（＝`(角距離 - 0.55r) / 速度` が最小の個体）の
角距離 Δ を毎フレーム求め、両方が入る最小の可視角半径 `max(8°, min(15.5°, Δ + 0.55r + 余白))`
へスプリングで寄せる。
→ 台風が遠い時は 15.5°（元ゲームと同じ引き）、迫ると自然に寄る。
**引きすぎない上限を 15.5° に固定する**のが要点。元より広く見えると難度が下がって別ゲームになる。

タイトル→開始時だけ WORLD → PLAY を 3 秒の slerp で繋ぐ手動演出を入れる。

### 5.3 酔い対策

- カメラの角速度に上限を掛ける
- **ロール（バンク）は入れない**。§5.1 の通り常に北が上
- 速度に応じた画角変化などの「速度演出」はしない
- ゲームオーバーのスローモーション（`dt *= 0.3`, 1500 ms）中もカメラは等速で追従する

---

## 6. 入力（**確定** — 元の操作系をそのまま移植）

元ゲームの操作は「**速度ベクトルの直接指定**」であり、旋回や加速度はない。維持する。

| 操作 | 元 | 本作 |
| --- | --- | --- |
| タッチ | ティッカー/ポップアップ/メニュー以外の**任意の場所**を押すと、そこに仮想スティック（半径44px）が出現。`(vx,vy) = clamp(drag)/44` | 同一 |
| キーボード | WASD / 矢印。`vx,vy ∈ {-1,0,1}` の生値 | 同一 |
| 速度 | `jp += (vx,vy) * M*0.125 * dt` | `(vx,vy)` を**接平面上の (東, 南) 成分**として解釈し、§2.3 の大圏ステップで進める |

「斜め入力が √2 倍速い」という元の性質は**バグではなく仕様として残す**
（キーボードとスティックで最大速度が変わってしまうが、元がそうなっている）。

`(vx, vy)` → 接平面ベクトル: `dir = east * vx + north * (-vy)`（画面下方向が南）。
`east = ŷ × p` を正規化、`north = p × east`。極では §5.1 と同じ縮退処理。

既存リポジトリと同じく、同一ビルドが `@media (pointer: coarse)` でタッチUIへ切り替わる。

---

## 7. 画面上の英語文言 **[確認済: 元文言の対訳]**

元の日本語文言に 1:1 で対応させる。創作しない。

| 元 | 英語 |
| --- | --- |
| `ドラッグで日本列島を動かし\nタイフーンから逃げよう` | `Drag to move Taiwan.\nEscape the typhoons.` |
| `夏を始める` | `Start the Summer` |
| `新しい夏を始める` | `Start a New Summer` |
| `𝕏 でシェア` | `Share on 𝕏` |
| `2026年8月1日`（HUD/タイトル） | `August 1, 2026` |
| `タイフーン{N}号（{NAME}）が{REGION}に上陸` | `Typhoon No. {N} ({NAME}) made landfall in {REGION}` |
| `タイフーン{N}号（{NAME}）が発生しました。今後の進路に警戒してください` | `Typhoon No. {N} ({NAME}) has formed. Stay alert for its forecast track.` |
| `タイフーン{N}号（{NAME}）は温帯低気圧に変わりました` | `Typhoon No. {N} ({NAME}) has weakened into an extratropical cyclone.` |
| `タイフーン{N}号（{NAME}）は日本から遠ざかり、消滅しました` | `Typhoon No. {N} ({NAME}) has moved away from Taiwan and dissipated.` |
| ティッカータグ `発生` | `FORMED` |
| ティッカータグ `消滅` | `DISSIPATED` |
| `メニュー` / `閉じる` | `Menu` / `Close` |
| `使用データ・ライセンス` | `Data sources and licenses` |
| `地図データが埋め込まれていません` | `Map data is not embedded.` |
| シェア本文 | `[Game] Move Taiwan and outrun the typhoons!` <br> `On {date}, Typhoon No. {N} ({NAME}) made landfall in {REGION}. I survived {D} days.` <br> `#TyphoonEscape` <br> `{url}` |

進路パターン名（内部識別子。UIには出ないが、コードとテストで使う）:
`直進 → STRAIGHT` / `迷走 → ERRATIC` / `転向 → RECURVING` / `追尾 → TRACKING` / `停滞 → STALLING`

地域名は §1b.5 の 6 種。

**未確定 [不明]**: 元は台風名を**カタカナ 140 個**で持っている。英語UIではローマ字表記に
すべきだが、**140 個のローマ字綴りを記憶で書くことはしない**。
実装時に気象庁または ESCAP/WMO 台風委員会の公開リストを取得して機械的に対応付け、
取得できなければカタカナのまま残して理由を明記する（§11-1）。

---

## 8. ディレクトリ構成（**確定**）

既存の Tower of Green Pillars とは完全に分離した独立 vite プロジェクトにする。

```
games/typhoon-escape/
  package.json          # 独立。deps: three / devDeps: vite, world-atlas, topojson-client, playwright-core
  vite.config.js        # base: "./"（既存と同じくどこに置いても動く）
  index.html
  DESIGN.md             # この文書
  CREDITS.md            # Natural Earth / world-atlas / 気象庁アジア名の出典とライセンス
  src/
    main.js             # 起動・状態遷移（title → play → over）とループ
    world/
      sphere.js         # 緯度経度⇔ベクトル、大圏移動、角距離、接平面基底  ← 純関数
      body.js           # 動く陸塊（台湾／他大陸）。クォータニオン姿勢と慣性  ← 純関数
      storm.js          # 台風の状態・進路更新・予報円・成長と衰弱          ← 純関数
      geo.js            # 焼き込み地図データの復号とリング取得
    rules/
      config.js         # §1b.3 の換算表そのもの。**唯一の数値の置き場所**
      game.js           # スポーン・当たり判定・スコア（生存日数）・上陸地域
      region.js         # §1b.5 の地域カスケード
      names.js          # 台風名リスト（生成物）
    render/
      globe.js          # 海・陸・海岸線・大気
      stormview.js      # 渦・予報円・過去進路（§4.3 の表）
      camera.js         # 追従と自動フレーミング
      hud.js            # 日付・ポップアップ・ティッカー・メニュー（英語）
      input.js          # 仮想スティックとキーボード
    data/
      geo.js            # 生成物（コミットする・手で編集しない）
    style.css
  tools/
    calibrate.mjs       # §1b の換算表を world-atlas 実データから再生成（設計値の出所）
    bake-geo.mjs        # world-atlas → src/data/geo.js
    bake-names.mjs      # 台風名リスト → src/rules/names.js
    standalone.mjs      # dist を1枚のHTMLへ
  test/
    harness.mjs         # 既存リポジトリと同じ依存ゼロの極小ハーネス
    sphere.test.mjs
    geo.test.mjs
    storm.test.mjs
    rules.test.mjs
    layering.test.mjs
    play.test.mjs       # playwright-core による実ブラウザ通し
    mobile.test.mjs
```

`npm run bake:geo` / `dev` / `build` / `test` / `standalone` を用意する。

---

## 9. テスト方針（**確定**）

既存リポジトリの「依存ゼロのハーネス＋playwright-core」を踏襲する。
グローブものは**目視では壊れに気付けない**ので、数学と*元ゲームとの一致*を機械的に検査する。

| テスト | 内容 |
| --- | --- |
| `sphere` | 緯度経度往復の誤差 < 1e-9 / 大圏移動後の `|p|=1`, `p·t=0` / §2.2 の既知距離と一致 / **極を跨ぐ移動と極上でのカメラ基底が破綻しない** |
| `geo` | 台湾10mの範囲が 118.281–122.007 / 21.905–25.287 と **10 m 以内**で一致 / 頂点558・リング8 / `land-50m` が 60,629点・1,421リング / 全リングが閉じている / 南極が除外されている |
| `storm` | **5パターンの定数が §1.4 の表と完全一致**（回帰防止） / 予報円 `(fx,fy,fr)` が `timer` 秒後の実位置・実半径と一致する（元の `setForecast` と同じ意味を持つことの検査） / 成長・衰弱・寿命の遷移 / `t.ang` 更新式の角度ラップ |
| `rules` | **速度逆転が t=10.0 日**（§1.6） / 発生間隔が t=31.25 日で 1.5 s に張り付く / 同時7個の上限 / 生存日数が `floor(elapsed)` / **§1b.1 の 3 つの無次元比が元の値と 1e-6 以内で一致** |
| `region` | §1b.5 の 6 地域すべてに台湾10m頂点が存在する / 主要都市15点の分類が §1b.5 の表と一致 |
| `calibrate` | `tools/calibrate.mjs` の出力と `rules/config.js` の値が一致する（**設計書と実装の乖離を検出する**） |
| `layering` | `src/world/**` と `src/render/**` のソースに `from "../rules` が現れないこと |
| `play` | 実ブラウザで起動 → 操作 → 被弾 → ポップアップまで通る / WebGL コンテキストのエラーがない / 1500 ms のスローモーション後にポップアップが出る |
| `mobile` | 縦画面・小画面でティッカーが増えてもスティックが押せる（`--rows` の持ち上げ） / HUD が重ならない |

`rules` の「無次元比の一致」検査が本設計の**移植の正しさを守る唯一の防波堤**である。
定数をうっかり触ると即座に落ちる。

---

## 10. 実装順序

ルール仕様が確定したので、**全工程が着手可能**。

1. プロジェクト雛形 + `bake-geo.mjs` + `geo.test.mjs`
   → 地図データが正しく焼けていることを先に固める
2. `sphere.js` + `sphere.test.mjs`（描画なしで数学だけ確定させる）
3. `rules/config.js` + `rules.test.mjs`
   → **§1b.3 の換算表と §1b.1 の比を先に固定する**。以降の全実装がこれを参照する
4. `storm.js` + `storm.test.mjs` — 進路・予報円・寿命を描画なしで通す
5. `globe.js` — 静止した地球を出す。ここで初めて絵が出る
6. `camera.js` — 追従と自動フレーミング
7. `body.js` + `input.js` — 台湾を動かせる状態、大陸の押しのけ
8. `stormview.js` — 予報円・進路・渦
9. `hud.js`（日付・ティッカー・ポップアップ・メニュー）/ シェア / 台風名
10. `mobile` 対応・`standalone` 化・テスト仕上げ

3 を 4 より先に置くのが要点。数値をコードに散らすと移植の正しさが検証できなくなる。

---

## 11. 未解決の確認事項

1. **台風名 140 個のローマ字表記** **[不明]**
   記憶で書かない。実装時に気象庁／台風委員会の公開リストを取得して機械変換する。
   取得できなければカタカナのまま残し、その旨をメニューに明記する。
2. **上陸地域の区分** **[推定]**
   §1b.5 は幾何近似。中華民國の公式区分に合わせるなら出典を引いて再定義する。
   また `Kinmen` / `Penghu` を独立地域にするか、まとめて `the Outlying Islands` にするか。
3. **開始日 2026-08-01 と「1秒=1日」** — 元のまま維持する前提で書いている。変更するなら §1b.3 全体が動く。
4. **台湾の描画倍率 `K=1.6`** — 元の日本と同じ値を採った。
   実寸で描くと致死円（95–162 km）に対して台湾（外接半径 214 km）が小さすぎ、難度が上がる。
   実寸にしたい場合は §1b.2 から再計算する。
5. **他大陸を押しのける機構を残すか** — 残す前提で設計した（§1b.4）。
   球面での実装コストは局所心射投影の追加のみで小さい。
6. 音（既存リポジトリは WebAudio 合成 + 録音声）。**元ゲームには音がない** **[確認済]**。
   付けるなら合成のみでよいか。
7. シェアURL — 元は `lovewcycle.com` を指す。本作のURLに差し替える必要がある。
